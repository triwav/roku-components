sub init()
	' m.grid = m.top.findNode("grid")
	' m.grid.setFocus(true)
	' parent = m.top
	' for i = 0 to 500
	' 	item = createObject("roSgNode", "OpenGridItem")
	' 	item.index = i
	' 	if parent.width <> invalid then

	' 		item.translation = [parent.width, parent.height]
	' 		print "set translation" item.translation
	' 	end if

	' 	' Do this last for performance reasons
	' 	parent.appendChild(item)
	' 	parent = item
	' end for

	' animation = m.top.createChild("Animation")
	' interpolator = animation.createChild("Vector2DFieldInterpolator")
	' interpolator.fieldToInterp = "item20.translation"
	' interpolator.key = [0, 1.0]
	' interpolator.keyValue = [[0, 0], [-8000, -4000]]
	' animation.duration = 20


	' animation.control = "start"
end sub
